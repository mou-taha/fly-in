[project]
name = "fly-in"
version = "0.1.0"
description = "An efficient drone routing system "
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.13.4",
]
[tool.black]
line-length = 79